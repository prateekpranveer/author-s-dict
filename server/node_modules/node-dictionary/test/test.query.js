const { expect } = require('chai')

const { api, formatUrl, callback } = require('../lib/query')

const { tested, expected } = require('./mock/query')

describe('query', () => {
  it('should succeed to format url', () => {
    expect(formatUrl(api, tested.word)).to.equal(expected.url)
  })

  it('should return an array includes explains after resolve', () => {
    expect(callback(tested.explains)).to.deep.equal(expected.result)
  })
})
