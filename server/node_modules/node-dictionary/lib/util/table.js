const table = require('text-table')
const wcwidth = require('wcwidth')

module.exports = stringArrays =>
  table(stringArrays.map(stringArray => {
    stringArray.unshift('')

    return stringArray
  }), { stringLength: wcwidth })
